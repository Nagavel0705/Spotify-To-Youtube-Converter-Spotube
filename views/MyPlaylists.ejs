<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Spotube</title>
  <link rel="stylesheet" href="style.css">
</head>

<body>
  <h1 style="margin-top: 60px;">My Spotify Playlists</h1>
  <div class="my-playlists-div">
    <% if(playlists.length === 0) { %>
      <p style="margin-top: 20px;">You don't have any Spotify playlists yet</p>
    <% } %> 
    <% playlists.forEach(playlist=> { %>
      <form action="/getUserPlaylistSongs" method="POST">
        <div class="playlist-elements" onclick="this.parentNode.submit()">
          <input type="hidden" name="playlistImg" value="<%= playlist[0] %>">
          <input type="hidden" name="playlistName" value="<%= playlist[1] %>">
          <input type="hidden" name="playlistId" value="<%= playlist[2] %>">
          <img class="playlist-banner" src="<%= playlist[0] %>" alt="Playlist banner">
          <p class="playlist-name">
            <%= playlist[1] %>
          </p>
        </div>
      </form>
      <% }); %>
  </div>
</body>

</html>